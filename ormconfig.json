{
  "type": "postgres",
  "url": "${POSTGRES_URL}",
  "entities": [
    "src/**/*entity.ts"
  ],
  "migrations": [
    "src/migrations/**/*.ts"
  ],
  "cli": {
    "migrationsDir": "src/migrations"
  }
}
